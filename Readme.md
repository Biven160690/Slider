# Let's start with the npm i modules setup.
# Let's build our project npm run dev && npm run build.
# Launch the npm start project.
# To test the sliders, go to the browser devtool and select the mobile phone mode.